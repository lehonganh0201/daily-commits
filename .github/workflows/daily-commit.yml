name: Daily Random Commits

on:
  schedule:
    - cron: '0 12 * * *'  # Chạy lúc 12h trưa UTC (5PM VN, +7). Chỉnh giờ nếu muốn.
  workflow_dispatch:  # Cho phép chạy thủ công để test

jobs:
  random-commits:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Random số lần commit (1–5) và commit
      run: |
        # Random số commit từ 1 đến 5
        NUM_COMMITS=$((RANDOM % 5 + 1))
        echo "Hôm nay sẽ commit $NUM_COMMITS lần!"
        
        # Loop để tạo số commit ngẫu nhiên
        for ((i=1; i<=NUM_COMMITS; i++)); do
          echo "$(date '+%Y-%m-%d %H:%M:%S') - Commit thứ $i: Chăm chỉ code đây! 😎" >> README.md
          git add README.md
          git commit -m "Daily auto commit $i/$NUM_COMMITS: $(date '+%Y-%m-%d %H:%M:%S')"
        done

    - name: Push changes
      run: |
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}